const Kinds = require("../middlewares/models/Kind");

const getAllKinds = async (req, res, next) => {
  try {
    const series = await Serie.find();

    res.json({ series });
  } catch (error) {
    next(error);
  }
};


module.exports = {
  getAllSeries,
};
